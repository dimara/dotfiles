# C-a :source .screenrc

source .screenrc

screen -t mbox mutt -f +mbox
screen -t gmail mutt -f +dimara-gmail -F ~/.mutt/private/google.rc
screen -t sent mutt -f +sent
screen -t today mutt -f +today
screen -t bash

select 0
